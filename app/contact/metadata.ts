import { defaultMetadata } from "@/lib/metadata"
import type { Metadata } from "next"

export const metadata: Metadata = {
  ...defaultMetadata,
  title: "Contact | Breakwater Marine Solutions",
  description: "Reach out to Breakwater Marine Solutions for port logistics, vessel coordination, and marine operations across Eastern Canada.",
}
